export interface IProductCount {
  // title: string;
  // id: string;
  productCount: number;
}

export interface IFilterItem {
  hex: string | undefined;
  title: string;
  id: string;
  productCount: number;
}
